import { SetDirectionDirective } from './set-direction.directive';

describe('SetDirectionDirective', () => {
  it('should create an instance', () => {
    const directive = new SetDirectionDirective();
    expect(directive).toBeTruthy();
  });
});
